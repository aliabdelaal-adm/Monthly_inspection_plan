# 📂 دليل نظام إدارة ملفات التقارير الذكي

## نظرة عامة

نظام إدارة ملفات التقارير الذكي هو أداة متقدمة مصممة خصيصاً للمطورين لإدارة تقارير التفتيش الجماعي بشكل كامل. يوفر النظام واجهة سهلة الاستخدام للرفع، التعديل، الحذف، والتحميل المباشر للتقارير في مستودع GitHub.

## ✨ المميزات الرئيسية

### 1. **للمطورين فقط**
- الواجهة مخفية تماماً عن المستخدمين العاديين
- تظهر فقط للمطورين المصرح لهم (isDev = true)
- حماية كاملة بنظام التوكن

### 2. **إدارة شاملة للملفات**
- ✅ **رفع ملفات جديدة**: رفع تقارير PDF، Excel، Word، PowerPoint
- ✏️ **تعديل البيانات**: تعديل العنوان والوصف لأي تقرير
- 🗑️ **حذف ملفات**: حذف نهائي من GitHub والنظام
- 📥 **تحميل سهل**: تحميل مباشر من GitHub

### 3. **التكامل التلقائي**
- يتم حفظ جميع الملفات في مجلد `files/group_inspection_reports/`
- تحديث تلقائي لملف `files.json` عند كل عملية
- ظهور فوري للتقارير في صفحة index.html للجميع

### 4. **دعم أنواع ملفات متعددة**
- PDF (📄)
- Excel: .xlsx, .xls (📊)
- Word: .docx, .doc (📝)
- PowerPoint: .pptx, .ppt (📊)

## 🚀 كيفية الاستخدام

### الطريقة الأولى: من index.html

1. افتح صفحة `index.html`
2. سجل الدخول كمطور
3. انقر على أيقونة "تقارير التفتيش الجماعي" 📑
4. ستظهر لوحة "إدارة ملفات التقارير الذكية" (للمطور فقط)
5. انقر على زر "فتح لوحة إدارة التقارير الذكية" 🚀
6. سيتم فتح `smart-planner.html` مباشرة على تبويب إدارة الملفات

### الطريقة الثانية: من smart-planner.html مباشرة

1. افتح صفحة `smart-planner.html`
2. سجل الدخول بـ GitHub Token
3. انقر على زر "إدارة ملفات التقارير الجماعية" في القائمة الرئيسية
4. أو اختر تبويب "📂 إدارة ملفات التقارير" من شريط التبويبات

## 📤 رفع تقرير جديد

### الخطوات:

1. في تبويب إدارة الملفات، املأ النموذج:
   - **اختر ملف التقرير**: حدد الملف من جهازك (حد أقصى 25 ميجابايت)
   - **عنوان التقرير**: أدخل عنواناً واضحاً ومعبراً
   - **وصف التقرير**: أضف وصفاً تفصيلياً (اختياري)
   - **اسم الرافع**: اسمك أو اسم المطور (يمكن تركه كما هو)

2. انقر على زر "📤 رفع التقرير"

3. سيتم:
   - رفع الملف إلى GitHub في المسار `files/group_inspection_reports/`
   - إضافة معلومات الملف تلقائياً إلى `files.json`
   - ظهور التقرير فوراً في قائمة التقارير
   - إمكانية عرض التقرير للجميع في index.html

### معلومات إضافية:
- يتم فحص صلاحية GitHub Token تلقائياً
- يتم التحقق من حجم الملف قبل الرفع
- في حالة وجود ملف بنفس الاسم، سيتم استبداله

## ✏️ تعديل تقرير موجود

### الخطوات:

1. في قائمة التقارير، ابحث عن التقرير المطلوب
2. انقر على زر "✏️ تعديل" بجانب التقرير
3. سيظهر مربع حوار لتعديل:
   - عنوان التقرير
   - وصف التقرير
4. أدخل البيانات الجديدة واضغط OK
5. سيتم تحديث `files.json` تلقائياً

**ملاحظة**: التعديل يطبق على البيانات الوصفية فقط، الملف نفسه لا يتغير.

## 🗑️ حذف تقرير

### الخطوات:

1. في قائمة التقارير، ابحث عن التقرير المطلوب
2. انقر على زر "🗑️ حذف" بجانب التقرير
3. سيظهر تأكيد: "هل أنت متأكد من حذف هذا التقرير؟"
4. اضغط OK للتأكيد

### ماذا يحدث عند الحذف:
- ✅ حذف الملف من GitHub نهائياً
- ✅ إزالة معلومات الملف من `files.json`
- ✅ اختفاء التقرير من index.html للجميع

**تحذير**: الحذف نهائي ولا يمكن التراجع عنه!

## 📥 تحميل تقرير

### طريقتان للتحميل:

#### 1. من smart-planner.html (للمطور):
- انقر على زر "📥 تحميل" بجانب أي تقرير
- سيتم فتح نافذة جديدة مع رابط التحميل المباشر من GitHub

#### 2. من index.html (للجميع):
- افتح modal تقارير التفتيش الجماعي
- انقر على زر "📥 تحميل التقرير" لأي تقرير متاح
- التحميل متاح للجميع (المطورين والمستخدمين)

## 🔄 تحديث قائمة التقارير

لتحديث قائمة التقارير وعرض أحدث التغييرات:
1. انقر على زر "🔄 تحديث القائمة" في أعلى قائمة التقارير
2. سيتم تحميل أحدث البيانات من `files.json`

## 🔐 متطلبات الأمان

### للمطورين:
1. **GitHub Token**: يجب أن يكون لديك توكن GitHub صالح
2. **الصلاحيات**: يجب أن يكون التوكن لديه صلاحيات:
   - `repo` (للقراءة والكتابة)
   - `contents` (للتعامل مع الملفات)

### التحقق من الصلاحيات:
- يتم التحقق تلقائياً من صلاحية التوكن قبل أي عملية
- في حالة انتهاء التوكن، سيظهر تنبيه مع تعليمات التحديث

## 📊 الهيكل التقني

### ملفات النظام:

```
Monthly_inspection_plan/
├── index.html                          # الصفحة الرئيسية مع زر المطور
├── smart-planner.html                  # صفحة إدارة الملفات الذكية
├── files.json                          # قاعدة بيانات الملفات
├── files/
│   └── group_inspection_reports/       # مجلد تقارير التفتيش الجماعي
│       ├── report1.pdf
│       ├── report2.xlsx
│       └── ...
└── test_smart_file_manager.html        # ملف اختبار النظام
```

### بنية files.json:

```json
{
  "files": [
    {
      "id": "group_report_1234567890",
      "name": "تقرير_التفتيش_الجماعي.pdf",
      "path": "files/group_inspection_reports/تقرير_التفتيش_الجماعي.pdf",
      "category": "group_inspection_reports",
      "type": "pdf",
      "size": 1234567,
      "uploadDate": "2025-11-26T12:00:00.000Z",
      "description": "تقرير التفتيش الجماعي - نوفمبر 2025",
      "uploader": "د. علي عبدالعال",
      "visible": true
    }
  ],
  "categories": [...],
  "lastUpdate": "2025-11-26T12:00:00.000Z"
}
```

## 🧪 اختبار النظام

تم إنشاء ملف اختبار شامل: `test_smart_file_manager.html`

### محتويات الاختبار:
1. ✅ اختبار مكونات index.html
2. ✅ اختبار مكونات smart-planner.html
3. ✅ اختبار الدوال الأساسية في JavaScript
4. ✅ اختبار المميزات الإضافية

### كيفية تشغيل الاختبار:
1. افتح `test_smart_file_manager.html` في المتصفح
2. انقر على زر "▶️ تشغيل جميع الاختبارات"
3. راجع النتائج في الملخص

## 🎯 حالات الاستخدام

### حالة 1: رفع تقرير شهري جديد
```
المطور → index.html → تقارير التفتيش الجماعي → 
فتح لوحة إدارة التقارير الذكية → رفع تقرير جديد → 
اختيار ملف PDF + كتابة العنوان والوصف → رفع التقرير
```

### حالة 2: تحديث معلومات تقرير موجود
```
المطور → smart-planner.html → تبويب إدارة الملفات → 
قائمة التقارير → زر تعديل → تحديث العنوان/الوصف → حفظ
```

### حالة 3: حذف تقرير قديم
```
المطور → smart-planner.html → تبويب إدارة الملفات → 
قائمة التقارير → زر حذف → تأكيد الحذف
```

### حالة 4: مستخدم عادي يشاهد التقارير
```
المستخدم → index.html → تقارير التفتيش الجماعي → 
عرض قائمة التقارير المتاحة → تحميل أي تقرير
```

## 🔧 استكشاف الأخطاء

### مشكلة: "❌ يرجى تسجيل الدخول أولاً"
**الحل**: تأكد من تسجيل الدخول بـ GitHub Token صالح في smart-planner.html

### مشكلة: "❌ حجم الملف كبير جداً"
**الحل**: الحد الأقصى للملف هو 25 ميجابايت. قلل حجم الملف أو قسمه إلى أجزاء

### مشكلة: "❌ فشل رفع الملف إلى GitHub"
**الحل**: 
- تحقق من صلاحية GitHub Token
- تأكد من وجود صلاحيات الكتابة على المستودع
- تحقق من اتصالك بالإنترنت

### مشكلة: الزر لا يظهر في index.html
**الحل**: 
- تأكد من تسجيل الدخول كمطور
- تحقق من أن `isDev = true` أو `window.isDev = true`

## 📝 ملاحظات مهمة

1. **الأمان**: جميع العمليات محمية بنظام التوكن ولا تتاح إلا للمطورين
2. **الأداء**: يتم تحميل الملفات مباشرة من GitHub بدون وسيط
3. **التوافق**: يعمل النظام على جميع المتصفحات الحديثة
4. **التخزين**: جميع الملفات مخزنة بشكل دائم في GitHub
5. **الوصول**: التقارير متاحة للعرض والتحميل للجميع بعد رفعها

## 🌟 تحسينات مستقبلية محتملة

- [ ] دعم المعاينة المباشرة للملفات
- [ ] نظام تصنيف وفلترة متقدم
- [ ] تحميل ملفات متعددة دفعة واحدة
- [ ] إحصائيات استخدام التقارير
- [ ] نظام نسخ احتياطي تلقائي
- [ ] دعم البحث النصي داخل التقارير

## 👨‍💻 المطور

**د. علي عبدالعال** - مطور النظام الذكي لإدارة تقارير التفتيش الجماعي

---

**آخر تحديث**: نوفمبر 2025  
**الإصدار**: 1.0.0  
**الحالة**: ✅ جاهز للاستخدام
